export default class Song {
  constructor(id, title, artist, src, cover, video) {
    this.id = id;
    this.title = title;
    this.artist = artist;
    this.src = src;
    this.cover = cover;
    this.video = video;
  }
}